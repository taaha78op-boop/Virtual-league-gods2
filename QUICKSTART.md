# ⚡ راهنمای سریع (Quick Start)

## نصب در 5 دقیقه!

### گام 1: آپلود فایل‌ها
تمام فایل‌ها را در سرور آپلود کنید

### گام 2: ایجاد دیتابیس
```bash
mysql -u root -p
CREATE DATABASE football_league CHARACTER SET utf8mb4 COLLATE utf8mb4_persian_ci;
EXIT;
```

### گام 3: اجرای اسکریپت نصب
```bash
chmod +x install.sh
./install.sh
```

یا **به صورت دستی**:
1. فایل `database/setup.sql` را در دیتابیس Import کنید
2. فایل `config/database.php` را با اطلاعات دیتابیس خود ویرایش کنید

### گام 4: دسترسی به سیستم
به آدرس سایت خود بروید و با این اطلاعات وارد شوید:

**مدیر لیگ:**
- نقش: مدیر لیگ
- رمز عبور: `admin123`

**مدیر تیم:**
- نقش: مدیر تیم  
- انتخاب تیم: (هر تیمی که بسازید)
- رمز عبور: `team123`

**بیننده:**
- نقش: بیننده
- بدون رمز عبور

---

## 🎯 اولین قدم‌ها

### 1. ایجاد تیم‌ها
1. وارد پنل مدیر شوید
2. به بخش "مدیریت تیم‌ها" بروید
3. دکمه "افزودن تیم جدید" را بزنید
4. اطلاعات تیم را وارد کنید

### 2. افزودن بازیکنان
1. به بخش "مدیریت بازیکنان" بروید
2. دکمه "افزودن بازیکن" را بزنید
3. تیم، نام، و پست بازیکن را انتخاب کنید
4. آمار بازیکن را وارد کنید

### 3. ثبت مسابقه
1. به بخش "مسابقات" بروید
2. دکمه "مسابقه جدید" را بزنید
3. تیم‌ها، تاریخ و نتیجه را وارد کنید

### 4. مدیریت نقل و انتقالات
1. مدیران تیم‌ها درخواست می‌دهند
2. مدیر لیگ در بخش "نقل و انتقالات" تأیید یا رد می‌کند

---

## 🚨 نکات مهم

### امنیت
⚠️ **قبل از استفاده واقعی:**
- رمز عبور مدیر را تغییر دهید
- در Production از HTTPS استفاده کنید
- دسترسی به فایل‌های config را محدود کنید

### پشتیبان‌گیری
💾 **روزانه backup بگیرید:**
```bash
mysqldump -u root -p football_league > backup.sql
```

یا از دکمه "دانلود دیتابیس" در پایین صفحه استفاده کنید.

### عیب‌یابی
❓ **مشکل دارید؟**
1. Console مرورگر را چک کنید (F12)
2. لاگ‌های PHP را ببینید
3. اطمینان حاصل کنید MySQL در حال اجرا است
4. فایل INSTALL.md را مطالعه کنید

---

## 📞 پشتیبانی

اگر به مشکل برخوردید:
- فایل `INSTALL.md` را بخوانید
- فایل `README.md` را مطالعه کنید
- Console browser را بررسی کنید

---

**موفق باشید! 🎉**
