# 🚀 راهنمای نصب سیستم مدیریت لیگ فوتبال

## پیش‌نیازها

- **PHP 7.4+** با PDO و MySQL driver
- **MySQL 5.7+** یا **MariaDB 10.2+**
- **Apache** یا **Nginx** با mod_rewrite
- دسترسی به **phpMyAdmin** (اختیاری اما توصیه می‌شود)

## مراحل نصب

### 1. آپلود فایل‌ها

تمام فایل‌های پروژه را در دایرکتوری root سرور خود آپلود کنید:

```
/public_html/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── app.js
│   ├── auth.js
│   ├── admin.js
│   ├── team.js
│   ├── viewer.js
│   └── storage.js
├── api/
│   ├── teams.php
│   ├── players.php
│   ├── matches.php
│   ├── transfers.php
│   ├── budgets.php
│   ├── settings.php
│   ├── notifications.php
│   ├── data.php
│   └── export.php
├── config/
│   └── database.php
└── database/
    └── setup.sql
```

### 2. ایجاد دیتابیس

#### روش 1: از طریق phpMyAdmin

1. وارد phpMyAdmin شوید
2. یک دیتابیس جدید با نام `football_league` ایجاد کنید
3. فایل `database/setup.sql` را Import کنید

#### روش 2: از طریق Command Line

```bash
mysql -u root -p
CREATE DATABASE football_league CHARACTER SET utf8mb4 COLLATE utf8mb4_persian_ci;
USE football_league;
SOURCE database/setup.sql;
EXIT;
```

### 3. تنظیم اطلاعات دیتابیس

فایل `config/database.php` را باز کنید و اطلاعات زیر را ویرایش کنید:

```php
private $host = "localhost";        // آدرس سرور MySQL
private $db_name = "football_league"; // نام دیتابیس
private $username = "root";          // نام کاربری MySQL
private $password = "";              // رمز عبور MySQL
```

### 4. تنظیم مجوزها (Permissions)

مطمئن شوید فولدرها دسترسی مناسب دارند:

```bash
chmod 755 api/
chmod 755 config/
chmod 644 config/database.php
chmod 644 api/*.php
```

### 5. تنظیم .htaccess (برای Apache)

در دایرکتوری `api/` فایل `.htaccess` ایجاد کنید:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>

<IfModule mod_headers.c>
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header set Access-Control-Allow-Headers "Content-Type, Authorization"
</IfModule>
```

### 6. تست سیستم

1. مرورگر را باز کنید و به آدرس سایت خود بروید
2. صفحه لاگین باید نمایش داده شود
3. با اطلاعات پیش‌فرض وارد شوید:
   - **مدیر:** رمز عبور `admin123`
   - **تیم:** هر تیمی که ایجاد کنید با رمز `team123`

## اطلاعات پیش‌فرض

- **رمز مدیر:** `admin123`
- **بودجه پیش‌فرض هر تیم:** 50,000,000,000 تومان
- **نام لیگ:** لیگ برتر فوتبال
- **فصل:** 2024-2025

## تغییر رمز عبور مدیر

1. وارد پنل مدیر شوید
2. به بخش **تنظیمات** بروید
3. رمز عبور جدید را وارد کنید
4. ذخیره کنید

یا مستقیماً در دیتابیس:

```sql
UPDATE settings SET setting_value = 'YOUR_NEW_PASSWORD' WHERE setting_key = 'admin_password';
```

## رفع مشکلات رایج

### خطای اتصال به دیتابیس
- اطلاعات `config/database.php` را بررسی کنید
- مطمئن شوید سرور MySQL فعال است
- دسترسی کاربر را به دیتابیس چک کنید

### خطای 404 در API
- تنظیمات `.htaccess` را بررسی کنید
- `mod_rewrite` آپاچی را فعال کنید
- مسیر `api/` را چک کنید

### مشکل در نمایش زبان فارسی
- Charset دیتابیس باید `utf8mb4_persian_ci` باشد
- در PHP فایل‌ها `header('Content-Type: application/json; charset=utf-8')` اضافه شده باشد

### خطای CORS
- Header های CORS در فایل‌های PHP چک شوند
- در صورت نیاز `.htaccess` را تنظیم کنید

## امنیت

⚠️ **مهم:** قبل از استفاده در production:

1. رمز عبور مدیر را تغییر دهید
2. اطلاعات دیتابیس را در فایل `.env` قرار دهید (فایل نمونه اضافه کنید)
3. Prepared Statements در تمام Query ها استفاده شده (✓)
4. فایل `config/database.php` را از دسترسی وب محافظت کنید
5. SSL Certificate نصب کنید (HTTPS)
6. فایروال و محدودیت IP تنظیم کنید

## پشتیبان‌گیری

### Export از دیتابیس:
```bash
mysqldump -u root -p football_league > backup_$(date +%Y%m%d).sql
```

### یا از طریق UI:
روی دکمه "دانلود دیتابیس لیگ" در پایین صفحه کلیک کنید.

## پشتیبانی

در صورت بروز مشکل:
- لاگ‌های PHP را بررسی کنید: `tail -f /var/log/apache2/error.log`
- Console مرورگر را چک کنید (F12)
- اطمینان حاصل کنید تمام فایل‌ها آپلود شده‌اند

## ویژگی‌های سیستم

✅ مدیریت تیم‌ها و بازیکنان  
✅ ثبت مسابقات و نتایج  
✅ سیستم نقل و انتقالات  
✅ مدیریت بودجه  
✅ ترکیب بندی و چینش تیم‌ها  
✅ صفحه بیننده برای نمایش اطلاعات  
✅ Export و Import داده‌ها  
✅ نوتیفیکیشن و اعلانات  

---

**نسخه:** 1.0  
**آخرین بروزرسانی:** 2025
